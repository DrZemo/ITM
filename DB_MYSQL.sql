CREATE DATABASE ITM

USE ITM

CREATE TABLE USUARIOS(
ID BIGINT,
NOMBRES VARCHAR(20),
APELLIDOS VARCHAR(40),
CONTRASEÑA VARCHAR(40),
EMAIL VARCHAR(90),
DIRECCION VARCHAR(90),
EDAD INT,
PRIMARY KEY(ID)
);
